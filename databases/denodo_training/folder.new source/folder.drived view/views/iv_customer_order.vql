CREATE OR REPLACE VIEW iv_customer_order FOLDER = '/new source/drived view' AS SELECT bv_customer.customer_code_internal AS customer_code_internal, bv_customer.first_name AS first_name, bv_customer.last_name AS last_name, bv_customer.email AS email_address, bv_customer.phone AS phone, bv_customer.main_address AS main_address, bv_customer.main_postcode AS main_postcode, bv_customer.main_country AS main_country, bv_customer.registered_date AS registered_date, bv_order_item.order_id AS order_id, bv_order_item.product_id AS product_id, bv_orders.customer_code AS customer_code, bv_orders.status AS status, bv_orders.date_placed AS date_placed, bv_orders.date_delivered AS date_delivered, bv_orders.date_closed AS date_closed FROM (bv_orders AS bv_orders INNER JOIN bv_order_item AS bv_order_item ON bv_orders.id = bv_order_item.order_id ) INNER JOIN bv_customer AS bv_customer ON bv_orders.customer_code = bv_customer.customer_code_internal ;

ALTER VIEW iv_customer_order
 LAYOUT (bv_orders = [160, 100, 290, 146], bv_order_item = [630, 100, 280, 86], bv_customer = [650, 320, 300, 226]);

